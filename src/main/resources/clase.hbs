Voy a describirte el aspecto externo que tiene cualquier {{clase.nombre}}:

{{#if clase.superclase}}Todo objeto del tipo {{clase.nombre}} es una especialización de {{clase.superclase.nombre}}, por lo que es posibile utilizar un {{clase.nombre}} en cualquier sitio donde se requiera un {{clase.superclase.nombre}}.{{/if}}
{{#each clase.constructores}}
{{#unless argumentos}}Todo objeto del tipo {{clase.nombre}} puede crearse sin necesidad de proporcionar ninguna información adicional.{{/unless}}{{#if argumentos}}Todo objeto del tipo {{clase.nombre}} puede crearse proporcionando como información los siguientes tipos de campos:
{{#each argumentos}}    - {{this}}{{/each}}{{/if}}{{/each}}
{{#each clase.metodos}}
Todo objeto del tipo {{clase.nombre}} reacciona al mensaje {{nombre}}, el cual retorna {{tipoRetorno}}{{#unless parametros}} y no requiere parámetros{{/unless}}{{#if parametros}} y requiere como parámetros:
{{#each parametros}}    - {{this}}{{/each}}{{/if}}.{{/each}}